@page
@using presentacion_aspnetcore.Pages
@model CiudadesModel
@{
    ViewData["Title"] = "Ciudades";
}
<center>
    <div>
        <form method="POST"  @*Indica como se enviaran los datos*@
            enctype="multipart/form-data">
            @Html.AntiForgeryToken()     @*Esto es un token para darle privacidad a la pagina*@

            <div align="left">
                <h3>Ciudades</h3>
            </div>

            <div>
                <input type="submit"
                    style="margin-bottom: 15px"
                    value="Refrescar"
                    class="btn btn-secondary"         @*Crear botones para la utilizacion de paginas""*@
                    asp-page-handler="BtRefrescar" />
                <input type="submit"
                    style="margin-bottom: 15px"
                    value="Nuevo"
                    class="btn btn-secondary"
                    asp-page-handler="BtNuevo"
                    disabled="@(Model.Ciudad != null)"/>     @*Hace que el boton quede deshabilitado segun la condicion*@
                <input type="submit"
                    style="margin-bottom: 15px"
                    value="Guardar"
                    class="btn btn-secondary"
                    asp-page-handler="BtGuardar"
                    disabled="@(Model.Ciudad == null)"/>
            </div>

            @if (Model.Ciudad != null)
            {
                <input type="hidden" 
                    asp-for="@Model.Ciudad!.Id"/>     @*Sirve para guardar datos sin que el usuario lo vea*@

                <div>
                    <div class="form-group">
                        <label>
                            Nombre
                        </label>
                        <div class="col-6">
                            <input id="inNombre"
                                   class="cts_forms form-control"
                                   asp-for="@Model.Ciudad!.Nombre" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Poblacion
                        </label>
                        <div class="col-6">
                            <input id="inPoblacion"
                                   class="cts_forms form-control"
                                   asp-for="@Model.Ciudad!.Poblacion" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            FechaCreacion
                        </label>
                        <div class="col-6">
                            <input id="inFechaCreacion"
                                   class="cts_forms form-control"
                                   asp-for="@Model.Ciudad!.FechaCreacion" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            CodigoPostal
                        </label>
                        <div class="col-6">
                            <input id="inCodigoPostal"
                                   class="cts_forms form-control"
                                   asp-for="@Model.Ciudad!.CodigoPostal" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Estado
                        </label>
                        <div class="col-6">
                            <input id="inEstado"
                                   asp-for="@Model.Ciudad!.Estado" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Departamento
                        </label>
                        <div class="col-6">
                            @if (Model.ListaDepartamento != null)
                            {
                                <select class="form-control" @*Crea un dropdown, lista despliegable para abajo*@
                                        asp-for="@Model.Ciudad!.Departamento" @*Lo guarda en TipoPropiedad*@
                                        asp-items="@(new SelectList(Model.ListaDepartamento, "Id", "Nombre"))">
                                    @*Crea las opciones con parametro( lista de valores, guarda el id, y muestra el nombre)*@
                                    <option value="">-- Seleccione un tipo --</option> @*Placeholder o boton para desplegar*@
                                </select>
                            }
                        </div>
                    </div>
                </div>
            }
            
            @if (ViewData.ContainsKey("Mensaje"))
            {
                @await Html.PartialAsync("../Emergentes/Mensajes.cshtml");
            }

            @if (Model.Ciudad != null && Model.Borrando)
            {
                <div class='popup'>
                    <div class='pp_ms_inner'>
                        <div style='width:95%'>
                            <h2 align="left">
                                Mensaje
                            </h2>

                            <h6 align="left">
                                Desea borrar el Ciudads?
                            </h6>
                            <h6 align="left">
                                @Model.Ciudad!.Nombre
                            </h6>
                            <input type="submit"
                                   value="Cerrar"
                                   asp-page-handler="BtCerrar"
                                   class="btn btn-secondary" />
                            <input type="submit"
                                   value="Borrar"
                                   asp-page-handler="BtBorrar"
                                   class="btn btn-secondary" />
                        </div>
                    </div>
                </div>
            }

            @if (Model != null &&
                 Model.Lista != null &&
                 Model.Lista.Count > 0)
            {
                <table class="tb_data table table-striped table-bordered table-hover">
                    <tr>
                        <th>
                            Nombre
                        </th>
                        <th>
                            Poblacion
                        </th>
                        <th>
                            Fecha Creacion
                        </th>
                        <th>
                            Codigo Postal
                        </th>
                        <th class="cl_hidden">
                            Estado
                        </th>
                        <th class="cl_hidden">
                            Departamento
                        </th>
                    </tr>
                    @foreach (var elemento in Model.Lista)
                    {
                        <tr>
                            <th>
                                @elemento.Nombre
                            </th>
                            <th>
                                @elemento.Poblacion
                            </th>
                            <th>
                                @elemento.FechaCreacion.ToString("dd/MM/yyyy")
                            </th>
                            <th>
                                @elemento.CodigoPostal
                            </th>
                            <th class="cl_hidden">
                                @elemento.Estado
                            </th>
                            <th class="cl_hidden">
                                @(Model.ListaDepartamento!.FirstOrDefault(d => d.Id == elemento.Departamento)?.Nombre ?? "Sin tipo")
                            </th>
                            <th>
                                <input type="submit"
                                    value="Modificar"
                                    asp-page-handler="BtModificar"
                                    class="btn btn-secondary"
                                    asp-route-data="@elemento.Id" />
                                <input type="submit"
                                    value="Borrar"
                                    asp-page-handler="BtBorrarVal"
                                    class="btn btn-secondary"
                                    asp-route-data="@elemento.Id" />
                            </th>
                        </tr>
                    }
                </table>
            }
        </form>
    </div>
</center>